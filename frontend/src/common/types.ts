export interface ApiResult<T> {
  succeeded: boolean;
  errors: string[];
  data?: T;
}

export interface PaginatedList<T> {
  items: T[];
  pageNumber: number;
  totalPages: number;
  totalCount: number;
  hasPreviousPage: boolean;
  hasNextPage: boolean;
}